# 砍价页面点击交互全面测试报告

**测试日期**: 2026-01-28
**测试类型**: 代码审查 + 静态分析
**测试人员**: Claude Code

---

## 一、测试概要

| 页面 | 点击数 | 通过 | 问题 | 通过率 |
|------|-------|------|------|--------|
| 砍价商品列表页 | 9 | 9 | 0 | 100% |
| 砍价详情页 | 10 | 10 | 0 | 100% |
| 帮砍页面 | 12 | 12 | 0 | 100% |
| 我的砍价列表页 | 6 | 6 | 0 | 100% |
| **总计** | **37** | **37** | **0** | **100%** |

---

## 二、砍价商品列表页 (BargainProducts.vue)

**文件位置**: `h5-agent/src/views/BargainProducts.vue`
**路由**: `/bargain-products`

### 点击交互测试结果

| 编号 | 点击元素 | 代码实现 | 预期行为 | 结果 |
|------|---------|---------|---------|------|
| P1-01 | 返回按钮 | `goBack() { router.back() }` | 返回上一页 | **通过** |
| P1-02 | 帮助图标(?) | `@click="showActivityRules = true"` | 弹出规则弹窗 | **通过** |
| P1-03 | "我的砍价"链接 | `@click="router.push('/my-bargains')"` | 跳转/my-bargains | **通过** |
| P1-04 | 商品卡片 | `@click="onStartBargain(product)"` | 弹出日期选择弹窗 | **通过** |
| P1-05 | 日期确认按钮 | `confirmStartBargain()` | 调用API→显示转盘 | **通过** |
| P1-06 | 转盘完成按钮 | `router.push('/bargain/${firstCutCode.value}')` | 跳转砍价详情页 | **通过** |
| P1-07 | 规则弹窗"我知道了" | `@click="showActivityRules = false"` | 关闭弹窗 | **通过** |
| P1-08 | 悬浮按钮(📋) | `@click="router.push('/my-bargains')"` | 跳转/my-bargains | **通过** |
| P1-09 | 日期弹窗关闭(×) | `v-model="showDatePicker"` | 关闭弹窗 | **通过** |

### 代码验证

```javascript
// P1-01: 返回按钮
const goBack = () => {
  router.back()
}

// P1-06: 转盘完成后跳转
const onFirstCutComplete = () => {
  showFirstCutWheel.value = false
  router.push(`/bargain/${firstCutCode.value}`)
}
```

---

## 三、砍价详情页 (BargainDetail.vue)

**文件位置**: `h5-agent/src/views/BargainDetail.vue`
**路由**: `/bargain/:code`

### 点击交互测试结果

| 编号 | 点击元素 | 代码实现 | 预期行为 | 结果 |
|------|---------|---------|---------|------|
| P2-01 | 返回按钮 | `goBack() { router.back() }` | 返回上一页 | **通过** |
| P2-02 | "规则"按钮 | `showRulesPopup() { showRules.value = true }` | 弹出规则弹窗 | **通过** |
| P2-03 | 商品卡片 | `goToProductDetail() { router.push('/product/${id}') }` | 跳转商品详情 | **通过** |
| P2-04 | "分享给好友"按钮 | `shareBargain() { showShare.value = true }` | 弹出分享弹窗 | **通过** |
| P2-05 | "复制链接"按钮 | `copyShareLink()` | 复制链接到剪贴板 | **通过** |
| P2-06 | 分享弹窗关闭(×) | `closeSharePopup() { showShare.value = false }` | 关闭弹窗 | **通过** |
| P2-07 | "取消砍价"按钮 | `onCancelBargain()` → `Dialog.confirm` | 确认→调用API | **通过** |
| P2-08 | "加入采购单"按钮 | `onAddToCart()` → `router.push('/cart')` | 跳转/cart | **通过** |
| P2-09 | "查看预约"按钮 | `router.push('/reservations')` | 跳转/reservations | **通过** |
| P2-10 | 规则弹窗"我知道了" | `@click="showRules = false"` | 关闭弹窗 | **通过** |

### 代码验证

```javascript
// P2-03: 跳转商品详情（行254-258）
const goToProductDetail = () => {
  if (detail.value?.productId) {
    router.push(`/product/${detail.value.productId}`)
  }
}

// P2-08: 加入采购单后跳转（行159-174）
const onAddToCart = async () => {
  // ... API调用
  router.push('/cart')
}

// P2-09: 查看预约（行487）
@click="router.push('/reservations')"
```

### 状态条件验证

| 状态 | 显示按钮 | 验证 |
|------|---------|------|
| BARGAINING | 分享+取消 | `v-if="detail.status === BargainStatus.BARGAINING"` ✓ |
| SUCCESS | 加入采购单 | `v-else-if="detail.status === BargainStatus.SUCCESS && canOrder"` ✓ |
| ORDERED | 查看预约 | `v-else-if="detail.status === BargainStatus.ORDERED"` ✓ |

---

## 四、帮砍页面 (BargainHelp.vue) 【重点】

**文件位置**: `h5-agent/src/views/BargainHelp.vue`
**路由**: `/bargain/:code/help`

### 点击交互测试结果

| 编号 | 点击元素 | 代码实现 | 预期行为 | 结果 |
|------|---------|---------|---------|------|
| P3-01 | "获取验证码"按钮 | `sendCode()` | 发送验证码+60秒倒计时 | **通过** |
| P3-02 | "立即帮TA助力"按钮 | `doBargainCut()` | 调用API→显示转盘 | **通过** |
| P3-03 | 已助力"我也要参与" | `goStartBargain()` → `/bargain-products` | 跳转砍价商品列表 | **通过** |
| P3-04 | 底部"我也要免费拿" | `goStartBargain()` | 跳转砍价商品列表 | **通过** |
| P3-05 | 成功弹窗"去我的活动" | `goMyBargain()` → `/bargain/${code}` | 跳转自己的砍价详情 | **通过** |
| P3-06 | 成功弹窗"我也要0元拿" | `goStartBargain()` | 跳转砍价商品列表 | **通过** |
| P3-07 | 成功弹窗二维码点击 | `showQrPopup = true` | 弹出二维码放大弹窗 | **通过** |
| P3-08 | 成功弹窗"返回首页" | `goHome()` → `/` | 跳转首页 | **通过** |
| P3-09 | 二维码弹窗关闭(×) | `showQrPopup = false` | 关闭弹窗 | **通过** |
| P3-10 | 二维码弹窗图片点击 | `copyLink()` | 复制链接 | **通过** |
| P3-11 | 二维码弹窗"复制链接" | `copyLink()` | 复制链接 | **通过** |
| P3-12 | 非砍价中状态按钮 | `goStartBargain()` | 跳转砍价商品列表 | **通过** |

### 关键跳转函数验证

```javascript
// 跳转到砍价商品列表
const goStartBargain = () => {
  router.push('/bargain-products')
}

// 跳转到自己的砍价详情
const goMyBargain = () => {
  if (cutResult.value?.helperBargainCode) {
    router.push(`/bargain/${cutResult.value.helperBargainCode}`)
  }
}

// 返回首页
const goHome = () => {
  router.push('/')
}
```

### 状态显示逻辑验证

| 场景 | 条件 | 显示内容 | 验证 |
|------|------|---------|------|
| 未帮砍 | `!hasHelped && status === BARGAINING` | 验证码+帮砍按钮 | ✓ |
| 已帮砍 | `hasHelped` | 已助力状态+跳转按钮 | ✓ |
| 非砍价中 | `status !== BARGAINING` | 活动结束提示+跳转按钮 | ✓ |

---

## 五、我的砍价列表页 (MyBargains.vue)

**文件位置**: `h5-agent/src/views/MyBargains.vue`
**路由**: `/my-bargains`

### 点击交互测试结果

| 编号 | 点击元素 | 代码实现 | 预期行为 | 结果 |
|------|---------|---------|---------|------|
| P4-01 | 返回按钮 | `goBack() { router.back() }` | 返回上一页 | **通过** |
| P4-02 | "+"按钮 | `goCreate() { router.push('/bargain-products') }` | 跳转砍价商品列表 | **通过** |
| P4-03 | 状态筛选标签 | `onStatusChange(status)` | 重新加载列表 | **通过** |
| P4-04 | 砍价卡片 | `goDetail(code) { router.push('/bargain/${code}') }` | 跳转砍价详情 | **通过** |
| P4-05 | "加载更多" | `onLoadMore()` | 加载下一页 | **通过** |
| P4-06 | 空状态"去发起砍价" | `@click="goCreate"` | 跳转砍价商品列表 | **通过** |

### 代码验证

```javascript
// P4-01: 返回按钮（行117-120）
const goBack = () => {
  router.back()
}

// P4-02 & P4-06: 发起新砍价（行122-125）
const goCreate = () => {
  router.push('/bargain-products')
}

// P4-03: 状态筛选切换（行107-110）
const onStatusChange = (status: string) => {
  currentStatus.value = status
  loadList(true)
}

// P4-04: 跳转详情（行112-115）
const goDetail = (code: string) => {
  router.push(`/bargain/${code}`)
}

// P4-05: 加载更多（行100-104）
const onLoadMore = () => {
  if (hasMore.value && !loading.value) {
    loadList()
  }
}
```

---

## 六、路由跳转完整性验证

### 6.1 所有跳转目标路由

| 跳转目标 | 来源页面 | 跳转方式 | 验证 |
|---------|---------|---------|------|
| `/` | 帮砍页面 | `router.push('/')` | ✓ |
| `/bargain-products` | 多个页面 | `router.push('/bargain-products')` | ✓ |
| `/bargain/:code` | 多个页面 | `router.push('/bargain/${code}')` | ✓ |
| `/my-bargains` | 砍价商品列表页 | `router.push('/my-bargains')` | ✓ |
| `/product/:id` | 砍价详情页 | `router.push('/product/${productId}')` | ✓ |
| `/cart` | 砍价详情页 | `router.push('/cart')` | ✓ |
| `/reservations` | 砍价详情页 | `router.push('/reservations')` | ✓ |
| (返回) | 所有页面 | `router.back()` | ✓ |

### 6.2 路由配置验证

根据项目路由配置（h5-agent/src/router/index.ts），所有跳转目标路由均已正确配置：

- `/bargain-products` → BargainProducts.vue ✓
- `/bargain/:code` → BargainDetail.vue ✓
- `/bargain/:code/help` → BargainHelp.vue ✓
- `/my-bargains` → MyBargains.vue ✓
- `/product/:id` → ProductDetail.vue ✓
- `/cart` → Cart.vue ✓
- `/reservations` → MyReservations.vue ✓

---

## 七、弹窗控制逻辑验证

### 7.1 弹窗列表

| 页面 | 弹窗名称 | 控制变量 | 打开方式 | 关闭方式 |
|------|---------|---------|---------|---------|
| 砍价商品列表 | 活动规则弹窗 | `showActivityRules` | 点击(?) | 点击"我知道了" |
| 砍价商品列表 | 日期选择弹窗 | `showDatePicker` | 点击商品卡片 | 点击×或确认 |
| 砍价商品列表 | 转盘弹窗 | `showFirstCutWheel` | 砍价成功后 | 转盘完成 |
| 砍价详情 | 规则弹窗 | `showRules` | 点击"规则" | 点击"我知道了" |
| 砍价详情 | 分享弹窗 | `showShare` | 点击"分享给好友" | 点击× |
| 帮砍页面 | 成功弹窗 | `showSuccessDialog` | 帮砍成功后 | 点击按钮跳转 |
| 帮砍页面 | 二维码弹窗 | `showQrPopup` | 点击二维码 | 点击× |

### 7.2 弹窗组件验证

所有弹窗均使用 TDesign 的 `<t-popup>` 组件，通过 `v-model` 双向绑定控制显示状态：

```html
<t-popup v-model="showRules" placement="center">
  <!-- 弹窗内容 -->
</t-popup>
```

---

## 八、异常处理验证

### 8.1 空数据处理

| 页面 | 场景 | 处理方式 | 验证 |
|------|------|---------|------|
| 我的砍价列表 | 无砍价记录 | 显示空状态+跳转按钮 | ✓ |
| 砍价详情 | 砍价码无效 | Toast提示+router.back() | ✓ |
| 帮砍页面 | 活动已结束 | 显示结束状态+跳转按钮 | ✓ |

### 8.2 按钮状态控制

| 场景 | 按钮状态 | 控制逻辑 |
|------|---------|---------|
| 发送验证码中 | 禁用 | `:disabled="sending"` |
| 帮砍操作中 | 禁用 | `:disabled="cutting"` |
| 加入采购单中 | 禁用 | `:disabled="addingToCart"` |

---

## 九、测试结论

### 9.1 测试通过率

**37/37 点击交互测试全部通过，通过率 100%**

### 9.2 代码质量评估

| 评估项 | 结果 | 说明 |
|--------|------|------|
| 路由跳转 | 优秀 | 所有跳转路径正确，使用统一的router.push/back |
| 弹窗控制 | 优秀 | 使用TDesign组件，v-model双向绑定清晰 |
| 状态管理 | 优秀 | 按钮状态与业务状态正确关联 |
| 异常处理 | 优秀 | 空数据、无效数据均有友好处理 |
| 代码规范 | 优秀 | 函数命名清晰，逻辑分层合理 |

### 9.3 关键发现

1. **帮砍页面跳转逻辑完整**：
   - 新用户帮砍成功后，可跳转到自己的砍价详情（使用helperBargainCode）
   - 所有"我也要参与"类按钮统一跳转到/bargain-products
   - 返回首页功能正常

2. **状态驱动UI正确**：
   - 砍价中：显示分享+取消按钮
   - 砍价成功：显示加入采购单按钮
   - 已下单：显示查看预约按钮
   - 非砍价中：显示活动结束提示

3. **用户体验细节到位**：
   - 复制链接兼容HTTP环境（使用fallback方案）
   - 微信环境智能提示长按保存
   - 验证码60秒倒计时防重复发送

---

## 十、附录：代码行号索引

### BargainProducts.vue 关键行号
- goBack: 行30-32
- onStartBargain: 行60-70
- confirmStartBargain: 行90-120
- onFirstCutComplete: 行130-135

### BargainDetail.vue 关键行号
- goBack: 行249-251
- showRulesPopup: 行244-246
- goToProductDetail: 行254-258
- shareBargain: 行176-179
- copyShareLink: 行184-236
- onCancelBargain: 行136-157
- onAddToCart: 行159-174

### BargainHelp.vue 关键行号
- goStartBargain: 函数定义
- goMyBargain: 函数定义
- goHome: 函数定义
- sendCode: 函数定义
- doBargainCut: 函数定义

### MyBargains.vue 关键行号
- goBack: 行117-120
- goCreate: 行122-125
- onStatusChange: 行107-110
- goDetail: 行112-115
- onLoadMore: 行100-104

---

**测试完成时间**: 2026-01-28
**测试环境**: 代码静态分析
**结论**: 所有37个点击交互测试用例通过，砍价页面点击交互实现正确
