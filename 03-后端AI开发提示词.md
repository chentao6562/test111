# 🤖 蒙庆烟花系统 - 后端AI开发提示词手册

> **使用说明**：本文档包含后端各模块的AI开发提示词，按模块拆分，每个模块包含开发提示词和测试提示词。  
> **适用模型**：Claude Opus 4.5 / Sonnet 4 / GPT-4 / DeepSeek  
> **技术栈**：NestJS 10 + TypeORM + MySQL 8.0 + Redis

---

## 📋 目录

1. [项目初始化](#模块1-项目初始化)
2. [认证模块](#模块2-认证模块)
3. [用户模块](#模块3-用户模块)
4. [商品模块](#模块4-商品模块)
5. [分类模块](#模块5-分类模块)
6. [订单模块](#模块6-订单模块)
7. [代理商模块](#模块7-代理商模块)
8. [库存模块](#模块8-库存模块)
9. [移库任务模块](#模块9-移库任务模块)
10. [分润结算模块](#模块10-分润结算模块)
11. [提现模块](#模块11-提现模块)
12. [管理员模块](#模块12-管理员模块)
13. [员工模块](#模块13-员工模块)
14. [系统配置模块](#模块14-系统配置模块)
15. [文件上传模块](#模块15-文件上传模块)
16. [数据统计模块](#模块16-数据统计模块)

---

## 通用项目背景（每次对话开头使用）

```
【项目背景】
我正在开发"蒙庆烟花"B2B代理商订货系统的后端服务。

业务场景：
- 内蒙古烟花鞭炮批发商（总批），面向代理商进行批发销售
- 代理商分两级：一级代理可发展二级代理
- 代理商通过小程序在线订货，到店自取或选择VIP锁货（提前移库）
- 分润规则：订单<¥399（一级10%/二级2%），≥¥399（一级15%/二级3%）

技术栈：
- 框架：NestJS 10 + TypeORM 0.3.x
- 数据库：MySQL 8.0
- 缓存：Redis 7.x
- 认证：uniCloud uni-id（小程序端）+ JWT（业务层）
- 部署：阿里云ECS

代码规范：
1. 使用TypeScript严格模式
2. 遵循NestJS最佳实践（模块化、依赖注入、DTO验证）
3. 使用class-validator进行参数校验
4. 统一响应格式：{ code: number, message: string, data?: any }
5. 错误处理使用自定义HttpException
6. 所有金额使用decimal(10,2)，前端传入单位为元
7. 时间使用MySQL datetime，返回ISO格式字符串

目录结构：
src/
├── modules/           # 业务模块
├── common/            # 公共模块（装饰器、过滤器、守卫等）
├── config/            # 配置
├── database/          # 数据库相关
└── main.ts
```

---

## 模块1: 项目初始化

### 开发提示词 BE-01

```
【任务】NestJS项目初始化

请帮我初始化蒙庆烟花后端项目，包括：

1. 项目基础配置
   - package.json（包含所需依赖）
   - tsconfig.json（严格模式）
   - nest-cli.json
   - .env.example（环境变量模板）
   - .gitignore

2. 核心依赖
   - @nestjs/core, @nestjs/common, @nestjs/platform-express
   - @nestjs/typeorm, typeorm, mysql2
   - @nestjs/jwt, @nestjs/passport, passport-jwt
   - @nestjs/config
   - @nestjs/cache-manager, cache-manager, cache-manager-redis-store
   - class-validator, class-transformer
   - bcryptjs, uuid
   - @nestjs/swagger（API文档）

3. 基础目录结构
   src/
   ├── modules/
   ├── common/
   │   ├── decorators/
   │   ├── filters/
   │   ├── guards/
   │   ├── interceptors/
   │   ├── pipes/
   │   └── utils/
   ├── config/
   ├── database/
   │   ├── entities/
   │   └── migrations/
   ├── app.module.ts
   └── main.ts

4. 公共模块实现
   a) 统一响应拦截器 (transform.interceptor.ts)
      - 成功响应：{ code: 0, message: 'success', data: any }
      
   b) 全局异常过滤器 (http-exception.filter.ts)
      - 业务异常：{ code: number, message: string }
      - 系统异常：{ code: 500, message: '服务器错误' }
      
   c) 业务异常类 (business.exception.ts)
      - 自定义错误码枚举
      
   d) 验证管道 (validation.pipe.ts)
      - 使用class-validator
      - 返回友好错误信息

5. 配置模块
   - 数据库配置（TypeORM）
   - Redis配置
   - JWT配置
   - 环境变量读取

6. main.ts
   - 全局管道、过滤器、拦截器注册
   - Swagger文档配置
   - CORS配置
   - 端口：3000

请提供完整可运行的代码。
```

### 测试提示词 BE-01-TEST

```
【任务】项目初始化测试

请帮我验证项目初始化是否正确：

1. 启动测试
   - npm run start:dev 能否正常启动
   - 访问 http://localhost:3000 返回什么

2. 数据库连接测试
   - TypeORM连接MySQL是否成功
   - 提供一个简单的健康检查接口 GET /health

3. Redis连接测试
   - cache-manager连接Redis是否成功
   - 提供一个简单的缓存测试接口

4. 响应格式测试
   - 成功响应格式是否正确
   - 异常响应格式是否正确
   - 参数校验失败响应格式

5. Swagger文档
   - 访问 /api-docs 能否看到文档

请提供测试用的curl命令或HTTP请求示例。
```

---

## 模块2: 认证模块

### 开发提示词 BE-02

```
【任务】认证模块开发

基于上述项目背景，请开发认证模块 (auth module)。

【业务需求】
1. 小程序端登录
   - 接收uniCloud的uni-id-token
   - 验证token有效性（调用uniCloud接口）
   - 查询/创建本地用户
   - 签发业务JWT

2. 管理后台登录
   - 账号密码登录
   - 密码bcrypt加密验证
   - 签发管理员JWT

3. 员工端登录（库管/货管）
   - 手机号+验证码登录（或uniCloud登录）
   - 验证员工身份
   - 签发员工JWT

4. Token管理
   - JWT有效期：7天
   - 支持刷新Token
   - Redis存储Token黑名单（退出登录）

【数据表】
已有表：users, admins, employees（参考项目背景中的表结构）

【接口设计】
POST /api/auth/uni-login         # uniCloud登录
POST /api/auth/admin/login       # 管理员登录
POST /api/auth/employee/login    # 员工登录
GET  /api/auth/profile           # 获取当前用户信息
POST /api/auth/refresh           # 刷新Token
POST /api/auth/logout            # 退出登录

【技术要求】
1. 创建以下文件：
   - auth.module.ts
   - auth.controller.ts
   - auth.service.ts
   - dto/uni-login.dto.ts
   - dto/admin-login.dto.ts
   - dto/employee-login.dto.ts
   - strategies/jwt.strategy.ts
   - guards/jwt-auth.guard.ts
   - guards/roles.guard.ts
   - decorators/current-user.decorator.ts
   - decorators/roles.decorator.ts

2. JWT Payload结构：
   {
     sub: number,      // 用户ID
     type: 'user' | 'admin' | 'employee',
     role?: string,    // admin角色或employee角色
     iat: number,
     exp: number
   }

3. uniCloud验证逻辑：
   - 调用uniCloud的checkToken接口
   - 获取uni_id和用户信息
   - 本地用户不存在则自动创建

4. 守卫装饰器：
   - @Public() 公开接口
   - @Roles('admin', 'warehouse') 角色限制

请提供完整代码实现。
```

### 测试提示词 BE-02-TEST

```
【任务】认证模块测试

请帮我测试认证模块的各个接口：

【测试用例】

1. 管理员登录测试
```bash
# 正确登录
curl -X POST http://localhost:3000/api/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'

# 错误密码
curl -X POST http://localhost:3000/api/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "wrong"}'

# 缺少参数
curl -X POST http://localhost:3000/api/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin"}'
```

2. 获取当前用户
```bash
# 有效Token
curl http://localhost:3000/api/auth/profile \
  -H "Authorization: Bearer <token>"

# 无Token
curl http://localhost:3000/api/auth/profile

# 无效Token
curl http://localhost:3000/api/auth/profile \
  -H "Authorization: Bearer invalid_token"
```

3. 退出登录
```bash
curl -X POST http://localhost:3000/api/auth/logout \
  -H "Authorization: Bearer <token>"
```

4. uniCloud登录模拟测试
   - 提供mock uniCloud验证的方式
   - 测试新用户自动创建
   - 测试老用户登录

【预期结果】
请说明每个测试用例的预期响应格式和状态码。

【单元测试】
请提供auth.service.spec.ts的单元测试代码，覆盖：
- adminLogin方法
- validateUniToken方法
- generateToken方法
- logout方法
```

---

## 模块3: 用户模块

### 开发提示词 BE-03

```
【任务】用户模块开发

基于项目背景，请开发用户模块 (user module)。

【业务需求】
1. 用户信息管理
   - 获取个人信息
   - 更新个人信息（昵称、头像、真实姓名）
   - 实名认证（身份证）

2. 地址管理
   - 地址列表
   - 添加地址
   - 修改地址
   - 删除地址
   - 设为默认地址

【数据表】
users表（已有）
user_addresses表（已有）

【接口设计】
GET    /api/user/info              # 获取用户信息
PUT    /api/user/info              # 更新用户信息
POST   /api/user/verify            # 实名认证

GET    /api/user/address           # 地址列表
POST   /api/user/address           # 添加地址
PUT    /api/user/address/:id       # 修改地址
DELETE /api/user/address/:id       # 删除地址
PUT    /api/user/address/:id/default  # 设为默认

【文件结构】
user/
├── user.module.ts
├── user.controller.ts
├── user.service.ts
├── dto/
│   ├── update-user.dto.ts
│   ├── verify-user.dto.ts
│   ├── create-address.dto.ts
│   └── update-address.dto.ts
└── entities/
    ├── user.entity.ts
    └── user-address.entity.ts

【业务规则】
1. 用户最多10个收货地址
2. 设为默认时，其他地址取消默认
3. 删除默认地址后，最新的地址自动成为默认
4. 实名认证只能做一次，认证后不可修改

【DTO示例】
UpdateUserDto:
- nickname?: string (2-20字符)
- avatar?: string (URL)
- realName?: string (2-10字符)

CreateAddressDto:
- name: string (联系人，必填)
- phone: string (手机号，必填)
- province?: string
- city?: string  
- district?: string
- detail: string (详细地址，必填)
- isDefault?: boolean

请提供完整代码实现。
```

### 测试提示词 BE-03-TEST

```
【任务】用户模块测试

【测试用例】

1. 获取用户信息
```bash
curl http://localhost:3000/api/user/info \
  -H "Authorization: Bearer <user_token>"
```
预期：返回用户完整信息，包括代理等级、余额等

2. 更新用户信息
```bash
# 更新昵称
curl -X PUT http://localhost:3000/api/user/info \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"nickname": "张三代理"}'

# 昵称过长（应失败）
curl -X PUT http://localhost:3000/api/user/info \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"nickname": "这是一个超过二十个字符的昵称测试"}'
```

3. 地址管理测试
```bash
# 添加地址
curl -X POST http://localhost:3000/api/user/address \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "张三",
    "phone": "13800138000",
    "province": "内蒙古",
    "city": "呼和浩特",
    "district": "赛罕区",
    "detail": "XX小区XX号楼",
    "isDefault": true
  }'

# 获取地址列表
curl http://localhost:3000/api/user/address \
  -H "Authorization: Bearer <user_token>"

# 修改地址
curl -X PUT http://localhost:3000/api/user/address/1 \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"detail": "新地址"}'

# 设为默认
curl -X PUT http://localhost:3000/api/user/address/2/default \
  -H "Authorization: Bearer <user_token>"

# 删除地址
curl -X DELETE http://localhost:3000/api/user/address/1 \
  -H "Authorization: Bearer <user_token>"

# 超过10个地址（应失败）
# 循环添加11个地址测试
```

4. 实名认证测试
```bash
# 首次认证
curl -X POST http://localhost:3000/api/user/verify \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"realName": "张三", "idCard": "150100199001011234"}'

# 重复认证（应失败）
curl -X POST http://localhost:3000/api/user/verify \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"realName": "李四", "idCard": "150100199001015678"}'
```

【单元测试】
请提供user.service.spec.ts，覆盖：
- getUserInfo
- updateUserInfo
- createAddress
- updateAddress
- deleteAddress
- setDefaultAddress
- verifyUser（包括重复认证场景）
```

---

## 模块4: 商品模块

### 开发提示词 BE-04

```
【任务】商品模块开发

基于项目背景，请开发商品模块 (product module)。

【业务需求】
1. 小程序端（代理商查看）
   - 商品列表（分页、分类筛选、搜索）
   - 商品详情
   - 热销商品
   - 推荐商品

2. 管理后台
   - 商品CRUD
   - 上下架
   - 批量操作

【数据表】
CREATE TABLE products (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  category_id INT NOT NULL,
  name VARCHAR(128) NOT NULL,
  subtitle VARCHAR(255),
  cover VARCHAR(255),
  images JSON,           -- ["url1", "url2"]
  video VARCHAR(255),
  description TEXT,
  content TEXT,          -- 富文本详情
  price DECIMAL(10,2) NOT NULL,
  market_price DECIMAL(10,2),
  cost_price DECIMAL(10,2),
  unit VARCHAR(16) DEFAULT '件',
  weight DECIMAL(10,2),
  spec VARCHAR(64),      -- 规格描述
  stock INT DEFAULT 0,
  lock_stock INT DEFAULT 0,  -- 锁定库存（未支付订单）
  warning_stock INT DEFAULT 10,
  sales INT DEFAULT 0,
  views INT DEFAULT 0,
  min_buy INT DEFAULT 1,
  max_buy INT DEFAULT 0,  -- 0不限制
  sort INT DEFAULT 0,
  is_hot TINYINT DEFAULT 0,
  is_new TINYINT DEFAULT 0,
  is_recommend TINYINT DEFAULT 0,
  status TINYINT DEFAULT 1,  -- 1上架 0下架
  created_at DATETIME,
  updated_at DATETIME
);

【接口设计】

# 小程序端
GET  /api/product/list          # 商品列表
GET  /api/product/detail/:id    # 商品详情
GET  /api/product/hot           # 热销商品
GET  /api/product/recommend     # 推荐商品
GET  /api/product/search        # 搜索商品

# 管理后台
GET    /api/admin/product/list       # 商品列表（含下架）
GET    /api/admin/product/:id        # 商品详情
POST   /api/admin/product            # 创建商品
PUT    /api/admin/product/:id        # 更新商品
DELETE /api/admin/product/:id        # 删除商品
PUT    /api/admin/product/:id/status # 上下架
POST   /api/admin/product/batch-status  # 批量上下架

【文件结构】
product/
├── product.module.ts
├── product.controller.ts       # 小程序端
├── product-admin.controller.ts # 管理后台
├── product.service.ts
├── dto/
│   ├── query-product.dto.ts
│   ├── create-product.dto.ts
│   └── update-product.dto.ts
└── entities/
    └── product.entity.ts

【查询参数】
QueryProductDto:
- page?: number (默认1)
- pageSize?: number (默认20，最大50)
- categoryId?: number
- keyword?: string
- status?: number (仅管理端)
- isHot?: boolean
- isNew?: boolean
- sortBy?: 'sales' | 'price' | 'created_at'
- sortOrder?: 'asc' | 'desc'

【业务规则】
1. 小程序端只返回status=1的商品
2. 商品详情访问时views+1（可用Redis缓存后批量更新）
3. 删除商品需要检查是否有未完成订单
4. 可用库存 = stock - lock_stock

【响应格式】
商品列表返回：
{
  code: 0,
  data: {
    list: Product[],
    total: number,
    page: number,
    pageSize: number
  }
}

请提供完整代码实现。
```

### 测试提示词 BE-04-TEST

```
【任务】商品模块测试

【准备测试数据】
请提供SQL插入5个测试商品的语句，包含不同分类、不同状态。

【测试用例】

1. 商品列表测试
```bash
# 基础列表
curl "http://localhost:3000/api/product/list"

# 分页
curl "http://localhost:3000/api/product/list?page=1&pageSize=10"

# 分类筛选
curl "http://localhost:3000/api/product/list?categoryId=1"

# 关键词搜索
curl "http://localhost:3000/api/product/list?keyword=礼花"

# 排序
curl "http://localhost:3000/api/product/list?sortBy=sales&sortOrder=desc"

# 组合查询
curl "http://localhost:3000/api/product/list?categoryId=1&keyword=礼花&sortBy=price&sortOrder=asc"
```

2. 商品详情测试
```bash
# 正常获取
curl "http://localhost:3000/api/product/detail/1"

# 不存在的商品
curl "http://localhost:3000/api/product/detail/9999"

# 下架商品（应返回错误）
curl "http://localhost:3000/api/product/detail/5"  # 假设5是下架商品
```

3. 热销/推荐商品
```bash
curl "http://localhost:3000/api/product/hot"
curl "http://localhost:3000/api/product/recommend"
```

4. 管理后台-创建商品
```bash
curl -X POST http://localhost:3000/api/admin/product \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "categoryId": 1,
    "name": "测试商品",
    "price": 99.00,
    "stock": 100,
    "cover": "https://example.com/cover.jpg",
    "images": ["https://example.com/1.jpg"],
    "description": "测试描述",
    "isHot": true
  }'
```

5. 管理后台-更新商品
```bash
curl -X PUT http://localhost:3000/api/admin/product/1 \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"price": 128.00, "stock": 200}'
```

6. 上下架测试
```bash
# 下架
curl -X PUT http://localhost:3000/api/admin/product/1/status \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status": 0}'

# 批量下架
curl -X POST http://localhost:3000/api/admin/product/batch-status \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"ids": [1, 2, 3], "status": 0}'
```

7. 删除商品
```bash
# 正常删除
curl -X DELETE http://localhost:3000/api/admin/product/1 \
  -H "Authorization: Bearer <admin_token>"

# 有未完成订单的商品（应失败）
# 需要先创建一个订单
```

【性能测试】
请提供一个测试商品列表接口性能的方案：
- 插入1000条商品数据的脚本
- 测试分页查询响应时间
- 测试搜索功能响应时间
```

---

## 模块5: 分类模块

### 开发提示词 BE-05

```
【任务】分类模块开发

基于项目背景，请开发商品分类模块 (category module)。

【业务需求】
1. 小程序端
   - 获取分类列表（只返回启用的）

2. 管理后台
   - 分类CRUD
   - 排序调整
   - 启用/禁用

【数据表】
CREATE TABLE categories (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(32) NOT NULL,
  icon VARCHAR(255),
  image VARCHAR(255),
  sort INT DEFAULT 0,
  status TINYINT DEFAULT 1,
  created_at DATETIME,
  updated_at DATETIME
);

【接口设计】
# 小程序端
GET  /api/category/list            # 分类列表（启用的）

# 管理后台
GET    /api/admin/category/list    # 分类列表（全部）
POST   /api/admin/category         # 创建分类
PUT    /api/admin/category/:id     # 更新分类
DELETE /api/admin/category/:id     # 删除分类

【文件结构】
category/
├── category.module.ts
├── category.controller.ts
├── category-admin.controller.ts
├── category.service.ts
├── dto/
│   ├── create-category.dto.ts
│   └── update-category.dto.ts
└── entities/
    └── category.entity.ts

【业务规则】
1. 分类名称不能重复
2. 删除分类前检查是否有商品关联
3. 分类按sort字段升序排列
4. 小程序端缓存分类数据（Redis，1小时过期）

请提供完整代码实现。
```

### 测试提示词 BE-05-TEST

```
【任务】分类模块测试

【测试用例】

1. 获取分类列表（小程序端）
```bash
curl "http://localhost:3000/api/category/list"
```
预期：只返回status=1的分类，按sort排序

2. 管理后台-创建分类
```bash
# 正常创建
curl -X POST http://localhost:3000/api/admin/category \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"name": "新分类", "sort": 10}'

# 重复名称（应失败）
curl -X POST http://localhost:3000/api/admin/category \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"name": "礼花弹", "sort": 10}'
```

3. 更新分类
```bash
curl -X PUT http://localhost:3000/api/admin/category/1 \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"name": "更新名称", "status": 0}'
```

4. 删除分类
```bash
# 无商品的分类
curl -X DELETE http://localhost:3000/api/admin/category/10 \
  -H "Authorization: Bearer <admin_token>"

# 有商品的分类（应失败）
curl -X DELETE http://localhost:3000/api/admin/category/1 \
  -H "Authorization: Bearer <admin_token>"
```

5. 缓存测试
- 第一次请求分类列表，记录响应时间
- 第二次请求，确认从Redis返回（响应时间应更短）
- 更新分类后，确认缓存已清除
```

---

## 模块6: 订单模块

### 开发提示词 BE-06

```
【任务】订单模块开发

基于项目背景，请开发订单模块 (order module)。这是核心模块，请特别注意事务处理和并发控制。

【业务需求】
1. 代理商端
   - 创建订单（从订货单提交）
   - 订单列表（分状态筛选）
   - 订单详情
   - 取消订单
   - 获取提货码

2. 库管端
   - 待备货列表
   - 开始备货
   - 完成备货
   - 扫码核销

3. 管理后台
   - 订单列表（多条件筛选）
   - 订单详情
   - 订单状态操作
   - 订单导出

【数据表】
-- orders表
CREATE TABLE orders (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(32) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  
  goods_amount DECIMAL(10,2) NOT NULL,      -- 商品总额
  lock_fee DECIMAL(10,2) DEFAULT 0,          -- VIP锁货费
  discount_amount DECIMAL(10,2) DEFAULT 0,   -- 优惠金额
  pay_amount DECIMAL(10,2) NOT NULL,         -- 应付金额
  paid_amount DECIMAL(10,2) DEFAULT 0,       -- 已付金额
  
  pickup_type ENUM('store', 'lock') DEFAULT 'store',  -- 到店自取/提前锁货
  
  contact_name VARCHAR(32),
  contact_phone VARCHAR(20),
  contact_address VARCHAR(255),
  
  pickup_code VARCHAR(32),      -- 提货码
  pickup_qrcode VARCHAR(255),   -- 提货二维码URL
  
  status ENUM('unpaid','paid','preparing','prepared','transferring','waiting','completed','cancelled') DEFAULT 'unpaid',
  pay_status TINYINT DEFAULT 0,  -- 0未付 1已付 2部分付
  pay_type VARCHAR(16),
  
  agent_id BIGINT,               -- 关联代理商（用于分润）
  commission_status TINYINT DEFAULT 0,  -- 0待结算 1已结算
  
  remark VARCHAR(255),
  admin_remark VARCHAR(255),
  cancel_reason VARCHAR(255),
  
  paid_at DATETIME,
  prepared_at DATETIME,
  completed_at DATETIME,
  cancelled_at DATETIME,
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  INDEX idx_user_id (user_id),
  INDEX idx_status (status),
  INDEX idx_created_at (created_at)
);

-- order_items表
CREATE TABLE order_items (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_id BIGINT NOT NULL,
  product_id BIGINT NOT NULL,
  product_name VARCHAR(128) NOT NULL,
  product_cover VARCHAR(255),
  product_spec VARCHAR(64),
  price DECIMAL(10,2) NOT NULL,
  quantity INT NOT NULL,
  amount DECIMAL(10,2) NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  
  INDEX idx_order_id (order_id)
);

【订单状态流转】
unpaid(待支付) 
  → paid(已支付) 
    → preparing(备货中) 
      → prepared(已备货)
        → [到店自取] waiting(待提货) → completed(已完成)
        → [提前锁货] transferring(移库中) → waiting(待提货) → completed(已完成)

取消：unpaid/paid → cancelled

【接口设计】

# 代理商端
POST /api/order/create           # 创建订单
GET  /api/order/list             # 订单列表
GET  /api/order/detail/:id       # 订单详情
POST /api/order/cancel/:id       # 取消订单
GET  /api/order/pickup-code/:id  # 获取提货码

# 库管端
GET  /api/warehouse/order/prepare     # 待备货列表
POST /api/warehouse/order/:id/start   # 开始备货
POST /api/warehouse/order/:id/complete  # 完成备货
POST /api/warehouse/order/verify      # 扫码核销

# 管理后台
GET  /api/admin/order/list       # 订单列表
GET  /api/admin/order/:id        # 订单详情
PUT  /api/admin/order/:id/status # 更新状态
PUT  /api/admin/order/:id/remark # 更新备注
POST /api/admin/order/export     # 导出订单

【创建订单DTO】
CreateOrderDto:
- items: Array<{ productId: number, quantity: number }>
- pickupType: 'store' | 'lock'
- contactName: string
- contactPhone: string
- contactAddress?: string
- remark?: string

【文件结构】
order/
├── order.module.ts
├── order.controller.ts           # 代理商端
├── order-warehouse.controller.ts # 库管端
├── order-admin.controller.ts     # 管理后台
├── order.service.ts
├── dto/
│   ├── create-order.dto.ts
│   ├── query-order.dto.ts
│   └── cancel-order.dto.ts
└── entities/
    ├── order.entity.ts
    └── order-item.entity.ts

【核心业务逻辑】

1. 创建订单流程：
   a) 验证商品存在且上架
   b) 验证库存充足
   c) 计算订单金额
   d) 计算VIP锁货费（如果选择锁货）
   e) 锁定库存（product.lock_stock += quantity）
   f) 生成订单号（规则：MQ + 年月日 + 6位序号）
   g) 生成提货码（8位数字）
   h) 创建订单和订单商品
   i) 记录代理商关系（用于分润）
   j) 以上操作在事务中完成

2. 取消订单流程：
   a) 验证订单状态可取消
   b) 释放锁定库存
   c) 更新订单状态
   d) 记录取消原因

3. 核销订单流程：
   a) 扫描提货码
   b) 验证订单状态
   c) 更新订单状态为completed
   d) 更新支付状态
   e) 触发分润计算（异步）
   f) 扣减实际库存

4. VIP锁货费规则：
   - 固定费用：80元
   - 订单满500元免锁货费

5. 库存并发控制：
   - 使用Redis分布式锁
   - 或使用MySQL乐观锁（版本号）

请提供完整代码实现，特别注意事务处理和异常回滚。
```

### 测试提示词 BE-06-TEST

```
【任务】订单模块测试

【测试准备】
1. 确保有测试用户和测试商品
2. 准备管理员和库管员Token

【测试用例】

1. 创建订单测试
```bash
# 正常创建（到店自取）
curl -X POST http://localhost:3000/api/order/create \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {"productId": 1, "quantity": 2},
      {"productId": 2, "quantity": 1}
    ],
    "pickupType": "store",
    "contactName": "张三",
    "contactPhone": "13800138000"
  }'

# VIP锁货（不满500，有锁货费）
curl -X POST http://localhost:3000/api/order/create \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [{"productId": 1, "quantity": 1}],
    "pickupType": "lock",
    "contactName": "张三",
    "contactPhone": "13800138000",
    "contactAddress": "XX小区"
  }'

# VIP锁货（满500，免锁货费）
# 需要调整商品数量使总额>=500

# 库存不足（应失败）
curl -X POST http://localhost:3000/api/order/create \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [{"productId": 1, "quantity": 9999}],
    "pickupType": "store",
    "contactName": "张三",
    "contactPhone": "13800138000"
  }'

# 商品不存在（应失败）
curl -X POST http://localhost:3000/api/order/create \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [{"productId": 9999, "quantity": 1}],
    "pickupType": "store",
    "contactName": "张三",
    "contactPhone": "13800138000"
  }'
```

2. 订单列表测试
```bash
# 全部订单
curl "http://localhost:3000/api/order/list" \
  -H "Authorization: Bearer <user_token>"

# 按状态筛选
curl "http://localhost:3000/api/order/list?status=unpaid" \
  -H "Authorization: Bearer <user_token>"
```

3. 取消订单测试
```bash
# 取消待支付订单
curl -X POST http://localhost:3000/api/order/cancel/1 \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason": "不想要了"}'

# 取消已备货订单（应失败）
# 需要先将订单状态改为prepared
```

4. 库管操作测试
```bash
# 获取待备货列表
curl "http://localhost:3000/api/warehouse/order/prepare" \
  -H "Authorization: Bearer <warehouse_token>"

# 开始备货
curl -X POST http://localhost:3000/api/warehouse/order/1/start \
  -H "Authorization: Bearer <warehouse_token>"

# 完成备货
curl -X POST http://localhost:3000/api/warehouse/order/1/complete \
  -H "Authorization: Bearer <warehouse_token>"

# 扫码核销
curl -X POST http://localhost:3000/api/warehouse/order/verify \
  -H "Authorization: Bearer <warehouse_token>" \
  -H "Content-Type: application/json" \
  -d '{"pickupCode": "12345678"}'
```

5. 并发测试
```bash
# 使用ab或wrk进行并发测试
# 测试同时创建订单时库存是否正确扣减

ab -n 100 -c 10 -p order.json -T application/json \
  -H "Authorization: Bearer <token>" \
  http://localhost:3000/api/order/create
```

6. 事务回滚测试
- 模拟创建订单过程中数据库错误
- 验证库存是否正确回滚

【单元测试】
请提供order.service.spec.ts，覆盖：
- createOrder（成功、库存不足、商品下架）
- cancelOrder（成功、状态不允许）
- verifyOrder（成功、提货码错误、订单不存在）
- 并发创建订单的库存处理
```

---

## 模块7: 代理商模块

### 开发提示词 BE-07

```
【任务】代理商模块开发

基于项目背景，请开发代理商模块 (agent module)。

【业务需求】
1. 代理商申请
   - 普通用户申请成为代理商
   - 填写申请信息
   - 等待审核

2. 推广裂变
   - 获取推广码/二维码
   - 生成推广海报
   - 邀请记录

3. 我的团队
   - 直推列表
   - 团队统计

4. 管理后台
   - 代理商列表
   - 入驻审核
   - 层级关系查看
   - 代理商详情

【业务规则】
1. 代理等级
   - 0: 普通用户
   - 1: 一级代理（可发展下级）
   - 2: 二级代理

2. 层级关系
   - 一级代理由管理员直接审核通过
   - 二级代理通过一级代理的邀请码注册
   - 最多2级代理

3. 邀请码规则
   - 6位大写字母+数字
   - 唯一不重复
   - 审核通过后自动生成

【接口设计】

# 代理商端
POST /api/agent/apply            # 申请代理
GET  /api/agent/info             # 代理信息
GET  /api/agent/qrcode           # 推广码
GET  /api/agent/poster           # 推广海报
GET  /api/agent/team             # 我的团队
GET  /api/agent/team/stats       # 团队统计
GET  /api/agent/invites          # 邀请记录

# 管理后台
GET  /api/admin/agent/list       # 代理商列表
GET  /api/admin/agent/:id        # 代理商详情
PUT  /api/admin/agent/:id/audit  # 审核
PUT  /api/admin/agent/:id/level  # 调整等级
PUT  /api/admin/agent/:id/status # 禁用/启用
GET  /api/admin/agent/tree       # 层级树

【申请代理DTO】
ApplyAgentDto:
- realName: string (真实姓名)
- idCard: string (身份证)
- phone: string (联系电话)
- address?: string (经营地址)
- inviteCode?: string (邀请码，可选)

【文件结构】
agent/
├── agent.module.ts
├── agent.controller.ts
├── agent-admin.controller.ts
├── agent.service.ts
├── dto/
│   ├── apply-agent.dto.ts
│   └── audit-agent.dto.ts
└── entities/
    (使用user.entity.ts)

【核心逻辑】

1. 申请代理流程：
   a) 检查用户是否已是代理或已申请
   b) 如果有邀请码，验证邀请码有效性
   c) 保存申请信息，状态设为待审核
   d) 如果有邀请码，记录上级代理ID

2. 审核通过流程：
   a) 更新代理状态为通过
   b) 设置代理等级（有上级为2级，无上级为1级）
   c) 生成邀请码
   d) 设置成为代理时间

3. 邀请码生成：
   function generateInviteCode(): string {
     const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
     let code = '';
     for (let i = 0; i < 6; i++) {
       code += chars[Math.floor(Math.random() * chars.length)];
     }
     return code;
   }

4. 团队统计：
   - 直推人数：直接邀请的代理数量
   - 团队人数：所有下级代理数量
   - 团队业绩：所有下级产生的订单金额

请提供完整代码实现。
```

### 测试提示词 BE-07-TEST

```
【任务】代理商模块测试

【测试准备】
1. 创建几个测试用户
2. 准备管理员Token

【测试用例】

1. 申请代理测试
```bash
# 无邀请码申请（一级代理申请）
curl -X POST http://localhost:3000/api/agent/apply \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "realName": "张三",
    "idCard": "150100199001011234",
    "phone": "13800138000",
    "address": "呼和浩特市XX路XX号"
  }'

# 有邀请码申请（二级代理申请）
curl -X POST http://localhost:3000/api/agent/apply \
  -H "Authorization: Bearer <user2_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "realName": "李四",
    "idCard": "150100199001015678",
    "phone": "13900139000",
    "inviteCode": "ABC123"
  }'

# 重复申请（应失败）
# 使用同一用户再次申请

# 无效邀请码（应失败）
curl -X POST http://localhost:3000/api/agent/apply \
  -H "Authorization: Bearer <user3_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "realName": "王五",
    "idCard": "150100199001019999",
    "phone": "13700137000",
    "inviteCode": "INVALID"
  }'
```

2. 审核测试
```bash
# 通过审核
curl -X PUT http://localhost:3000/api/admin/agent/1/audit \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status": 2}'

# 拒绝审核
curl -X PUT http://localhost:3000/api/admin/agent/2/audit \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status": 3, "reason": "资料不完整"}'
```

3. 获取代理信息
```bash
curl http://localhost:3000/api/agent/info \
  -H "Authorization: Bearer <agent_token>"
```
预期：返回代理等级、邀请码、团队人数等

4. 获取推广码
```bash
curl http://localhost:3000/api/agent/qrcode \
  -H "Authorization: Bearer <agent_token>"
```
预期：返回小程序码图片URL

5. 我的团队
```bash
curl http://localhost:3000/api/agent/team \
  -H "Authorization: Bearer <agent_token>"
```
预期：返回直推代理商列表

6. 团队统计
```bash
curl http://localhost:3000/api/agent/team/stats \
  -H "Authorization: Bearer <agent_token>"
```
预期：
{
  directCount: 5,    // 直推人数
  teamCount: 12,     // 团队总人数
  teamSales: 15280   // 团队业绩
}

7. 代理商列表（管理后台）
```bash
# 全部列表
curl http://localhost:3000/api/admin/agent/list \
  -H "Authorization: Bearer <admin_token>"

# 按状态筛选
curl "http://localhost:3000/api/admin/agent/list?status=1" \
  -H "Authorization: Bearer <admin_token>"

# 按等级筛选
curl "http://localhost:3000/api/admin/agent/list?level=1" \
  -H "Authorization: Bearer <admin_token>"
```

8. 层级树
```bash
curl http://localhost:3000/api/admin/agent/tree \
  -H "Authorization: Bearer <admin_token>"
```
预期：返回树形结构的代理商层级关系
```

---

## 模块8: 库存模块

### 开发提示词 BE-08

```
【任务】库存模块开发

基于项目背景，请开发库存模块 (inventory module)。

【业务需求】
1. 库管端
   - 库存列表查询
   - 库存详情
   - 进货入库
   - 库存盘点

2. 管理后台
   - 库存概览
   - 库存明细
   - 库存流水
   - 库存调整
   - 库存预警

【数据表】
-- stock_logs表（库存流水）
CREATE TABLE stock_logs (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  product_id BIGINT NOT NULL,
  type ENUM('in','out','lock','unlock','adjust') NOT NULL,
  quantity INT NOT NULL,            -- 变动数量（正负）
  before_stock INT NOT NULL,        -- 变动前库存
  after_stock INT NOT NULL,         -- 变动后库存
  reason VARCHAR(32) NOT NULL,      -- 原因：purchase进货,sale销售,cancel取消,adjust调整
  relation_type VARCHAR(32),        -- 关联类型：order,purchase
  relation_id BIGINT,               -- 关联ID
  operator_type VARCHAR(16),        -- 操作人类型：admin,warehouse
  operator_id INT,
  remark VARCHAR(255),
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  
  INDEX idx_product_id (product_id),
  INDEX idx_type (type),
  INDEX idx_created_at (created_at)
);

-- purchases表（进货单）
CREATE TABLE purchases (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  purchase_no VARCHAR(32) NOT NULL UNIQUE,
  supplier_name VARCHAR(64),
  total_amount DECIMAL(10,2) NOT NULL,
  total_quantity INT NOT NULL,
  status ENUM('draft','pending','completed','cancelled') DEFAULT 'draft',
  operator_id INT,
  remark VARCHAR(255),
  completed_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- purchase_items表
CREATE TABLE purchase_items (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  purchase_id BIGINT NOT NULL,
  product_id BIGINT NOT NULL,
  product_name VARCHAR(128) NOT NULL,
  cost_price DECIMAL(10,2) NOT NULL,
  quantity INT NOT NULL,
  amount DECIMAL(10,2) NOT NULL,
  batch_no VARCHAR(32),
  production_date DATE,
  expiry_date DATE,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

【接口设计】

# 库管端
GET  /api/warehouse/stock/list       # 库存列表
GET  /api/warehouse/stock/:id        # 库存详情（单商品）
POST /api/warehouse/inbound          # 创建入库单
POST /api/warehouse/inbound/:id/confirm  # 确认入库
GET  /api/warehouse/inbound/list     # 入库单列表

# 管理后台
GET  /api/admin/inventory/overview   # 库存概览
GET  /api/admin/inventory/list       # 库存列表
GET  /api/admin/inventory/:id        # 库存详情
GET  /api/admin/inventory/logs       # 库存流水
PUT  /api/admin/inventory/:id/adjust # 库存调整
GET  /api/admin/inventory/warning    # 库存预警

GET  /api/admin/purchase/list        # 进货单列表
GET  /api/admin/purchase/:id         # 进货单详情
POST /api/admin/purchase             # 创建进货单
PUT  /api/admin/purchase/:id         # 更新进货单
POST /api/admin/purchase/:id/confirm # 确认入库

【文件结构】
inventory/
├── inventory.module.ts
├── inventory.controller.ts          # 库管端
├── inventory-admin.controller.ts    # 管理后台
├── purchase.controller.ts           # 进货管理
├── inventory.service.ts
├── purchase.service.ts
├── dto/
│   ├── query-stock.dto.ts
│   ├── adjust-stock.dto.ts
│   ├── create-inbound.dto.ts
│   └── create-purchase.dto.ts
└── entities/
    ├── stock-log.entity.ts
    ├── purchase.entity.ts
    └── purchase-item.entity.ts

【进货入库DTO】
CreateInboundDto:
- items: Array<{
    productId: number,
    quantity: number,
    costPrice: number,
    batchNo?: string,
    productionDate?: string,
    expiryDate?: string
  }>
- supplierName?: string
- remark?: string

【库存调整DTO】
AdjustStockDto:
- quantity: number (正数增加，负数减少)
- reason: string
- remark?: string

【核心逻辑】

1. 入库流程：
   a) 创建入库单（草稿状态）
   b) 确认入库：
      - 事务开始
      - 更新商品库存 product.stock += quantity
      - 记录库存流水 type='in', reason='purchase'
      - 更新入库单状态为completed
      - 事务提交

2. 库存调整：
   a) 管理员手动调整库存
   b) 记录调整原因
   c) 记录库存流水 type='adjust'

3. 库存预警：
   - 查询 stock <= warning_stock 的商品
   - 按库存从小到大排序

4. 库存流水查询：
   - 支持按商品、时间、类型筛选
   - 分页返回

请提供完整代码实现。
```

### 测试提示词 BE-08-TEST

```
【任务】库存模块测试

【测试用例】

1. 库存列表
```bash
# 库管端
curl http://localhost:3000/api/warehouse/stock/list \
  -H "Authorization: Bearer <warehouse_token>"

# 管理后台
curl http://localhost:3000/api/admin/inventory/list \
  -H "Authorization: Bearer <admin_token>"
```

2. 创建入库单
```bash
curl -X POST http://localhost:3000/api/warehouse/inbound \
  -H "Authorization: Bearer <warehouse_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {"productId": 1, "quantity": 100, "costPrice": 50.00},
      {"productId": 2, "quantity": 200, "costPrice": 30.00}
    ],
    "supplierName": "某烟花厂",
    "remark": "春节备货"
  }'
```

3. 确认入库
```bash
curl -X POST http://localhost:3000/api/warehouse/inbound/1/confirm \
  -H "Authorization: Bearer <warehouse_token>"
```
验证：
- 商品库存是否增加
- 库存流水是否正确记录

4. 库存调整
```bash
# 增加库存
curl -X PUT http://localhost:3000/api/admin/inventory/1/adjust \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"quantity": 10, "reason": "盘盈", "remark": "实际数量多于系统"}'

# 减少库存
curl -X PUT http://localhost:3000/api/admin/inventory/1/adjust \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"quantity": -5, "reason": "盘亏", "remark": "破损"}'
```

5. 库存流水
```bash
# 全部流水
curl http://localhost:3000/api/admin/inventory/logs \
  -H "Authorization: Bearer <admin_token>"

# 按商品筛选
curl "http://localhost:3000/api/admin/inventory/logs?productId=1" \
  -H "Authorization: Bearer <admin_token>"

# 按类型筛选
curl "http://localhost:3000/api/admin/inventory/logs?type=in" \
  -H "Authorization: Bearer <admin_token>"
```

6. 库存预警
```bash
curl http://localhost:3000/api/admin/inventory/warning \
  -H "Authorization: Bearer <admin_token>"
```

7. 事务测试
- 模拟入库过程中数据库错误
- 验证库存和流水是否回滚
```

---

## 模块9: 移库任务模块

### 开发提示词 BE-09

```
【任务】移库任务模块开发

基于项目背景，请开发移库任务模块 (transfer module)。

【业务背景】
当代理商选择"提前锁货"时，需要将商品从普通库位移至VIP库房。这个过程由"货管"（搬运师傅）执行。

【业务流程】
1. 代理商下单选择"提前锁货"
2. 订单备货完成后，自动生成移库任务
3. 货管在小程序看到任务，接单
4. 货管取货、搬运、扫码确认入库VIP
5. 任务完成，货管获得搬运费

【数据表】
CREATE TABLE transfer_tasks (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  task_no VARCHAR(32) NOT NULL UNIQUE,
  order_id BIGINT NOT NULL,
  
  porter_id INT,                    -- 货管ID
  assigned_at DATETIME,             -- 分配时间
  
  status ENUM('pending','assigned','picking','transferring','completed','cancelled') DEFAULT 'pending',
  
  transfer_fee DECIMAL(10,2) DEFAULT 0,  -- 搬运费
  fee_status TINYINT DEFAULT 0,          -- 0待结算 1已结算
  
  from_location VARCHAR(64),        -- 取货位置
  to_location VARCHAR(64) DEFAULT 'VIP库房',
  
  picked_at DATETIME,
  completed_at DATETIME,
  
  remark VARCHAR(255),
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- porter_incomes表（货管收入）
CREATE TABLE porter_incomes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  porter_id INT NOT NULL,
  task_id BIGINT NOT NULL,
  amount DECIMAL(10,2) NOT NULL,
  status ENUM('pending','settled','withdrawn') DEFAULT 'pending',
  settled_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

【接口设计】

# 货管端
GET  /api/porter/task/pending      # 待接单列表
GET  /api/porter/task/doing        # 进行中任务
GET  /api/porter/task/done         # 已完成任务
GET  /api/porter/task/:id          # 任务详情
POST /api/porter/task/:id/accept   # 接单
POST /api/porter/task/:id/pick     # 确认取货
POST /api/porter/task/:id/complete # 完成任务（扫码）
GET  /api/porter/income            # 收入统计
GET  /api/porter/income/list       # 收入明细

# 库管端
POST /api/warehouse/transfer/create    # 创建移库任务（通常自动创建）
GET  /api/warehouse/transfer/list      # 移库任务列表
POST /api/warehouse/transfer/:id/assign # 指派给货管

# 管理后台
GET  /api/admin/transfer/list      # 移库任务列表
GET  /api/admin/transfer/:id       # 任务详情
PUT  /api/admin/transfer/:id/assign # 指派
POST /api/admin/transfer/settle    # 批量结算

【文件结构】
transfer/
├── transfer.module.ts
├── transfer.controller.ts          # 货管端
├── transfer-warehouse.controller.ts # 库管端
├── transfer-admin.controller.ts    # 管理后台
├── transfer.service.ts
├── porter-income.service.ts
├── dto/
│   ├── create-transfer.dto.ts
│   └── query-transfer.dto.ts
└── entities/
    ├── transfer-task.entity.ts
    └── porter-income.entity.ts

【核心逻辑】

1. 任务状态流转：
   pending(待接单) → assigned(已分配) → picking(取货中) 
   → transferring(搬运中) → completed(已完成)

2. 自动创建任务：
   - 订单备货完成 && pickupType='lock'
   - 自动创建移库任务
   - 状态为pending

3. 接单逻辑：
   - 货管点击接单
   - 更新任务状态为assigned
   - 记录porter_id和assigned_at

4. 完成任务：
   - 扫描VIP库位码
   - 更新任务状态为completed
   - 更新订单状态为waiting（待提货）
   - 创建货管收入记录

5. 搬运费规则：
   - 固定每单40元（可配置）
   - 结算周期：每周/每月

请提供完整代码实现。
```

### 测试提示词 BE-09-TEST

```
【任务】移库任务模块测试

【测试准备】
1. 创建一个VIP锁货订单并完成备货
2. 准备货管Token

【测试用例】

1. 查看待接单任务
```bash
curl http://localhost:3000/api/porter/task/pending \
  -H "Authorization: Bearer <porter_token>"
```

2. 接单
```bash
curl -X POST http://localhost:3000/api/porter/task/1/accept \
  -H "Authorization: Bearer <porter_token>"
```
验证：任务状态变为assigned

3. 确认取货
```bash
curl -X POST http://localhost:3000/api/porter/task/1/pick \
  -H "Authorization: Bearer <porter_token>"
```
验证：任务状态变为picking

4. 完成任务
```bash
curl -X POST http://localhost:3000/api/porter/task/1/complete \
  -H "Authorization: Bearer <porter_token>" \
  -H "Content-Type: application/json" \
  -d '{"locationCode": "VIP001"}'
```
验证：
- 任务状态变为completed
- 订单状态变为waiting
- 创建了货管收入记录

5. 查看收入统计
```bash
curl http://localhost:3000/api/porter/income \
  -H "Authorization: Bearer <porter_token>"
```
预期：
{
  totalIncome: 200,      // 累计收入
  pendingIncome: 120,    // 待结算
  settledIncome: 80,     // 已结算
  todayIncome: 40        // 今日收入
}

6. 收入明细
```bash
curl http://localhost:3000/api/porter/income/list \
  -H "Authorization: Bearer <porter_token>"
```

7. 管理后台-指派任务
```bash
curl -X PUT http://localhost:3000/api/admin/transfer/1/assign \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"porterId": 1}'
```

8. 管理后台-批量结算
```bash
curl -X POST http://localhost:3000/api/admin/transfer/settle \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"taskIds": [1, 2, 3]}'
```
```

---

## 模块10: 分润结算模块

### 开发提示词 BE-10

```
【任务】分润结算模块开发

基于项目背景，请开发分润结算模块 (settlement/commission module)。

【业务背景】
代理商推广裂变的核心激励机制。当下级代理商或其团队成员完成订单后，上级代理商获得分润。

【分润规则】
1. 订单金额 < 399元：
   - 一级代理：10%
   - 二级代理：2%

2. 订单金额 >= 399元：
   - 一级代理：15%
   - 二级代理：3%

3. 分润层级：
   - 直接上级获得一级分润
   - 上级的上级获得二级分润
   - 最多2级

【数据表】
CREATE TABLE commissions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,          -- 受益人（代理商）
  order_id BIGINT NOT NULL,         -- 来源订单
  order_no VARCHAR(32) NOT NULL,
  from_user_id BIGINT NOT NULL,     -- 订单创建人
  
  order_amount DECIMAL(10,2) NOT NULL,   -- 订单金额
  commission_rate DECIMAL(5,4) NOT NULL, -- 分润比例
  commission_amount DECIMAL(10,2) NOT NULL, -- 分润金额
  
  level TINYINT NOT NULL,           -- 1直接 2间接
  status TINYINT DEFAULT 0,         -- 0待结算 1已结算 2已取消
  
  settled_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

【接口设计】

# 代理商端
GET  /api/commission/list          # 分润列表
GET  /api/commission/stats         # 分润统计
GET  /api/commission/balance       # 余额信息

# 管理后台
GET  /api/admin/commission/list    # 分润列表
GET  /api/admin/commission/stats   # 分润统计
POST /api/admin/commission/settle  # 批量结算

【文件结构】
settlement/
├── settlement.module.ts
├── commission.controller.ts       # 代理商端
├── commission-admin.controller.ts # 管理后台
├── commission.service.ts
├── dto/
│   └── query-commission.dto.ts
└── entities/
    └── commission.entity.ts

【核心逻辑】

1. 分润计算（订单完成时触发）：
   
   async calculateCommission(order: Order) {
     const threshold = await this.getConfig('commission_threshold'); // 399
     const rate1 = order.payAmount >= threshold 
       ? await this.getConfig('commission_rate_l1_t2')  // 0.15
       : await this.getConfig('commission_rate_l1_t1'); // 0.10
     const rate2 = order.payAmount >= threshold
       ? await this.getConfig('commission_rate_l2_t2')  // 0.03
       : await this.getConfig('commission_rate_l2_t1'); // 0.02

     // 获取订单创建人
     const user = await this.userRepo.findOne(order.userId);
     
     // 一级分润：直接上级
     if (user.parentId) {
       const parent = await this.userRepo.findOne(user.parentId);
       if (parent && parent.agentLevel > 0) {
         await this.createCommission({
           userId: parent.id,
           orderId: order.id,
           orderNo: order.orderNo,
           fromUserId: user.id,
           orderAmount: order.payAmount,
           commissionRate: rate1,
           commissionAmount: order.payAmount * rate1,
           level: 1
         });
       }
       
       // 二级分润：上级的上级
       if (parent?.parentId) {
         const grandParent = await this.userRepo.findOne(parent.parentId);
         if (grandParent && grandParent.agentLevel > 0) {
           await this.createCommission({
             userId: grandParent.id,
             orderId: order.id,
             orderNo: order.orderNo,
             fromUserId: user.id,
             orderAmount: order.payAmount,
             commissionRate: rate2,
             commissionAmount: order.payAmount * rate2,
             level: 2
           });
         }
       }
     }
   }

2. 结算逻辑：
   - 订单完成后，分润状态为待结算
   - 管理员批量结算后：
     a) 分润状态改为已结算
     b) 累加到代理商余额
     c) 更新代理商total_income

3. 统计查询：
   - 今日分润
   - 本月分润
   - 累计分润
   - 待结算分润

请提供完整代码实现。
```

### 测试提示词 BE-10-TEST

```
【任务】分润结算模块测试

【测试准备】
1. 创建代理商层级关系：A(一级) -> B(二级) -> C(普通用户)
2. 让C创建并完成订单

【测试用例】

1. 触发分润计算
```bash
# 模拟订单完成后的分润计算
# 可以通过核销订单来触发

# 订单金额 < 399
curl -X POST http://localhost:3000/api/warehouse/order/verify \
  -H "Authorization: Bearer <warehouse_token>" \
  -H "Content-Type: application/json" \
  -d '{"pickupCode": "12345678"}'
```
验证：
- A获得10%分润（一级）
- B获得2%分润（二级）

2. 查看分润列表（代理商端）
```bash
# A的分润
curl http://localhost:3000/api/commission/list \
  -H "Authorization: Bearer <A_token>"

# B的分润
curl http://localhost:3000/api/commission/list \
  -H "Authorization: Bearer <B_token>"
```

3. 分润统计
```bash
curl http://localhost:3000/api/commission/stats \
  -H "Authorization: Bearer <agent_token>"
```
预期：
{
  todayAmount: 34.40,
  monthAmount: 1280.50,
  totalAmount: 5680.00,
  pendingAmount: 344.00
}

4. 余额信息
```bash
curl http://localhost:3000/api/commission/balance \
  -H "Authorization: Bearer <agent_token>"
```
预期：
{
  balance: 980.00,        // 可提现余额
  frozenAmount: 0,        // 冻结金额
  totalIncome: 1280.50,   // 累计收入
  pendingAmount: 300.50   // 待结算
}

5. 管理后台-批量结算
```bash
curl -X POST http://localhost:3000/api/admin/commission/settle \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"ids": [1, 2, 3, 4, 5]}'
```
验证：
- 分润状态变为已结算
- 代理商余额增加

6. 不同金额订单的分润比例测试
```bash
# 订单金额 = 300 (< 399)
# 预期：一级10%=30，二级2%=6

# 订单金额 = 500 (>= 399)
# 预期：一级15%=75，二级3%=15
```
```

---

## 模块11: 提现模块

### 开发提示词 BE-11

```
【任务】提现模块开发

基于项目背景，请开发提现模块 (withdrawal module)。

【业务需求】
1. 代理商/货管申请提现
2. 管理员审核
3. 审核通过后，线下转账

【数据表】
CREATE TABLE withdrawals (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  withdraw_no VARCHAR(32) NOT NULL UNIQUE,
  user_type ENUM('user', 'porter') NOT NULL,
  user_id BIGINT NOT NULL,
  
  amount DECIMAL(10,2) NOT NULL,
  fee DECIMAL(10,2) DEFAULT 0,
  actual_amount DECIMAL(10,2) NOT NULL,  -- 实际到账
  
  bank_name VARCHAR(32),
  bank_account VARCHAR(32),
  bank_holder VARCHAR(32),
  
  status ENUM('pending','approved','rejected','completed') DEFAULT 'pending',
  
  auditor_id INT,
  audit_at DATETIME,
  audit_remark VARCHAR(255),
  
  completed_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

【接口设计】

# 代理商端
POST /api/withdrawal/apply         # 申请提现
GET  /api/withdrawal/list          # 提现记录

# 货管端
POST /api/porter/withdrawal/apply  # 申请提现
GET  /api/porter/withdrawal/list   # 提现记录

# 管理后台
GET  /api/admin/withdrawal/list    # 提现列表
GET  /api/admin/withdrawal/:id     # 提现详情
PUT  /api/admin/withdrawal/:id/audit  # 审核

【文件结构】
withdrawal/
├── withdrawal.module.ts
├── withdrawal.controller.ts       # 代理商端
├── withdrawal-porter.controller.ts # 货管端
├── withdrawal-admin.controller.ts # 管理后台
├── withdrawal.service.ts
├── dto/
│   ├── apply-withdrawal.dto.ts
│   └── audit-withdrawal.dto.ts
└── entities/
    └── withdrawal.entity.ts

【申请提现DTO】
ApplyWithdrawalDto:
- amount: number (提现金额)
- bankName: string (银行名称)
- bankAccount: string (银行账号)
- bankHolder: string (持卡人)

【审核DTO】
AuditWithdrawalDto:
- status: 'approved' | 'rejected'
- remark?: string

【业务规则】
1. 最低提现金额：100元（可配置）
2. 手续费：0%（可配置）
3. 提现金额不能超过可用余额
4. 审核通过后冻结金额，完成后扣减
5. 审核拒绝后解冻

【核心逻辑】

1. 申请提现：
   a) 验证金额>=最低提现额
   b) 验证金额<=可用余额
   c) 冻结余额（balance减少，frozen_amount增加）
   d) 创建提现记录

2. 审核通过：
   a) 更新状态为approved
   b) 记录审核信息
   c) 等待线下打款

3. 完成提现：
   a) 更新状态为completed
   b) 扣减冻结金额

4. 审核拒绝：
   a) 更新状态为rejected
   b) 解冻金额（frozen_amount减少，balance增加）

请提供完整代码实现。
```

### 测试提示词 BE-11-TEST

```
【任务】提现模块测试

【测试准备】
1. 确保代理商有足够余额
2. 准备管理员Token

【测试用例】

1. 申请提现
```bash
# 正常申请
curl -X POST http://localhost:3000/api/withdrawal/apply \
  -H "Authorization: Bearer <agent_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 200,
    "bankName": "建设银行",
    "bankAccount": "6222801234567890123",
    "bankHolder": "张三"
  }'

# 金额不足（应失败）
curl -X POST http://localhost:3000/api/withdrawal/apply \
  -H "Authorization: Bearer <agent_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 999999,
    "bankName": "建设银行",
    "bankAccount": "6222801234567890123",
    "bankHolder": "张三"
  }'

# 低于最低提现额（应失败）
curl -X POST http://localhost:3000/api/withdrawal/apply \
  -H "Authorization: Bearer <agent_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 50,
    "bankName": "建设银行",
    "bankAccount": "6222801234567890123",
    "bankHolder": "张三"
  }'
```
验证：余额冻结

2. 提现记录
```bash
curl http://localhost:3000/api/withdrawal/list \
  -H "Authorization: Bearer <agent_token>"
```

3. 管理后台-审核通过
```bash
curl -X PUT http://localhost:3000/api/admin/withdrawal/1/audit \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status": "approved"}'
```

4. 管理后台-完成提现
```bash
curl -X PUT http://localhost:3000/api/admin/withdrawal/1/complete \
  -H "Authorization: Bearer <admin_token>"
```
验证：冻结金额扣减

5. 管理后台-审核拒绝
```bash
curl -X PUT http://localhost:3000/api/admin/withdrawal/2/audit \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status": "rejected", "remark": "银行账号有误"}'
```
验证：冻结金额解冻回余额

6. 货管端提现
```bash
curl -X POST http://localhost:3000/api/porter/withdrawal/apply \
  -H "Authorization: Bearer <porter_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 100,
    "bankName": "工商银行",
    "bankAccount": "6222021234567890123",
    "bankHolder": "李四"
  }'
```
```

---

## 模块12-16: 其他模块

### 开发提示词 BE-12 (管理员模块)

```
【任务】管理员模块开发

请开发管理后台的管理员管理模块。

【功能】
1. 管理员CRUD
2. 角色权限：超管(1)、运营(2)、财务(3)
3. 密码修改
4. 登录日志

【接口】
GET    /api/admin/system/admin/list
POST   /api/admin/system/admin
PUT    /api/admin/system/admin/:id
DELETE /api/admin/system/admin/:id
PUT    /api/admin/system/admin/:id/password

【DTO】
CreateAdminDto:
- username: string
- password: string
- name: string
- phone?: string
- role: 1 | 2 | 3

UpdateAdminDto:
- name?: string
- phone?: string
- role?: number
- status?: number

请提供完整代码实现。
```

### 开发提示词 BE-13 (员工模块)

```
【任务】员工模块开发

请开发员工管理模块（库管/货管）。

【功能】
1. 员工CRUD
2. 角色：warehouse(库管)、porter(货管)
3. 启用/禁用

【接口】
GET    /api/admin/system/employee/list
POST   /api/admin/system/employee
PUT    /api/admin/system/employee/:id
DELETE /api/admin/system/employee/:id

【DTO】
CreateEmployeeDto:
- name: string
- phone: string
- role: 'warehouse' | 'porter'

请提供完整代码实现。
```

### 开发提示词 BE-14 (系统配置模块)

```
【任务】系统配置模块开发

请开发系统配置模块。

【功能】
1. 配置列表
2. 更新配置
3. 按分组获取配置

【数据表】
configs表（已定义）

【接口】
GET  /api/admin/system/config          # 配置列表
GET  /api/admin/system/config/:group   # 按组获取
PUT  /api/admin/system/config          # 批量更新

【配置项】
basic组：site_name, site_logo, contact_phone, store_address
order组：min_order_amount, lock_fee, lock_fee_free_amount
commission组：分润比例相关
withdraw组：withdraw_min, withdraw_fee_rate

【服务方法】
getConfig(key: string): Promise<any>  # 获取单个配置，支持缓存

请提供完整代码实现。
```

### 开发提示词 BE-15 (文件上传模块)

```
【任务】文件上传模块开发

请开发文件上传模块（阿里云OSS）。

【功能】
1. 单文件上传
2. 多文件上传
3. 图片压缩（可选）

【配置】
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=
OSS_REGION=
OSS_ENDPOINT=

【接口】
POST /api/upload/image           # 上传图片
POST /api/upload/images          # 批量上传

【限制】
- 单文件最大：10MB
- 允许格式：jpg, jpeg, png, gif, webp
- 返回完整URL

【技术实现】
使用 @nestjs/platform-express 的 FileInterceptor
使用 ali-oss SDK

请提供完整代码实现。
```

### 开发提示词 BE-16 (数据统计模块)

```
【任务】数据统计模块开发

请开发管理后台的数据统计模块。

【功能】
1. 仪表盘数据
2. 销售报表
3. 代理业绩报表
4. 商品分析

【接口】
GET /api/admin/dashboard/stats        # 今日概览
GET /api/admin/dashboard/chart        # 趋势图表
GET /api/admin/dashboard/todos        # 待办事项

GET /api/admin/report/sales           # 销售报表
GET /api/admin/report/agent           # 代理业绩
GET /api/admin/report/product         # 商品分析

【仪表盘数据】
{
  todaySales: number,        // 今日销售额
  todayOrders: number,       // 今日订单数
  todayAgents: number,       // 今日新增代理
  pendingOrders: number,     // 待处理订单
  
  salesTrend: [              // 7天销售趋势
    { date: '01-22', amount: 12580 },
    ...
  ],
  
  todos: [                   // 待办事项
    { type: 'audit', count: 3, label: '待审核代理申请' },
    { type: 'prepare', count: 5, label: '待备货订单' },
    ...
  ]
}

请提供完整代码实现。
```

---

## 附录：通用测试脚本

### 完整流程测试脚本

```bash
#!/bin/bash
# 蒙庆烟花系统完整流程测试脚本

BASE_URL="http://localhost:3000/api"

echo "=== 1. 管理员登录 ==="
ADMIN_TOKEN=$(curl -s -X POST $BASE_URL/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.data.token')
echo "Admin Token: $ADMIN_TOKEN"

echo "=== 2. 创建测试商品 ==="
curl -s -X POST $BASE_URL/admin/product \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "categoryId": 1,
    "name": "测试礼花弹",
    "price": 128,
    "stock": 100
  }'

echo "=== 3. 模拟用户登录（需要实际uniCloud token）==="
# USER_TOKEN=...

echo "=== 4. 创建订单 ==="
# curl -s -X POST $BASE_URL/order/create \
#   -H "Authorization: Bearer $USER_TOKEN" \
#   -H "Content-Type: application/json" \
#   -d '{...}'

echo "=== 完成 ==="
```

### 性能测试脚本

```bash
#!/bin/bash
# 性能测试脚本

# 商品列表接口压测
ab -n 1000 -c 50 \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:3000/api/product/list

# 创建订单接口压测（注意库存）
ab -n 100 -c 10 \
  -p order.json \
  -T application/json \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:3000/api/order/create
```

---

## 最佳实践提醒

1. **事务处理**
   - 涉及多表操作必须使用事务
   - 使用TypeORM的QueryRunner或@Transaction装饰器

2. **并发控制**
   - 库存操作使用Redis分布式锁
   - 或使用MySQL乐观锁

3. **缓存策略**
   - 商品列表、分类列表使用Redis缓存
   - 配置项使用Redis缓存
   - 更新时清除对应缓存

4. **错误处理**
   - 统一使用BusinessException
   - 返回友好的错误信息

5. **日志记录**
   - 关键操作记录日志
   - 使用NestJS Logger

6. **安全考虑**
   - 参数校验
   - SQL注入防护（TypeORM已处理）
   - XSS防护（输入过滤）

---

**文档完成！可以按模块顺序依次开发，每完成一个模块进行测试后再进行下一个。**
